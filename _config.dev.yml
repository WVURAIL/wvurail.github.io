# Local development overrides. Run with:
#   bundle exec jekyll serve --config _config.yml,_config.dev.yml
# This points URLs at localhost so absolute links work while developing.

url: "http://localhost:4000"
